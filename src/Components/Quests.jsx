// import "./Quests.css"; // Optional: Create a CSS file for styling if needed

const Quests = () => {
    return (
        <div className="quests">
            <h1>Quests</h1>
            <p>Explore and complete exciting quests!</p>
        </div>
    );
};

export default Quests;